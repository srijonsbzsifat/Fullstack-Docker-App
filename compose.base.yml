version: "3.8"

volumes:
  mongo-data: {}

services:
  # -------------------- App stack --------------------
  mongodb:
    image: mongo:latest
    container_name: mongodb
    env_file:
      - ./env/.env.shared
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db

  backend:
    build: ./backend
    container_name: backend-app
    env_file:
      - ./env/.env.shared
    environment:
      # identical to your old compose, just reading from env/.env.shared + env/.env.* at runtime
      MONGO_URI: mongodb://${MONGO_INITDB_ROOT_USERNAME}:${MONGO_INITDB_ROOT_PASSWORD}@mongodb:27017/${MONGO_DATABASE}?authSource=admin
      NODE_ENV: ${NODE_ENV}
    ports:
      - "${BACKEND_PORT}:${BACKEND_PORT}"
    depends_on:
      - mongodb

  frontend:
    build: ./frontend
    container_name: frontend-app
    env_file:
      - ./env/.env.shared
    environment:
      NODE_ENV: ${NODE_ENV}
    ports:
      - "${FRONTEND_PORT}:${FRONTEND_PORT}"
    depends_on:
      - backend
