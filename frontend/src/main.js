import { createApp } from 'vue'
import App from './App.vue'

// Create the Vue application instance and mount it to the element with id="app"
// This element is in the public/index.html file.
createApp(App).mount('#app')